var prodSearch = document.querySelector('#searchField').value;
var figures = document.querySelectorAll('.gallery');
var figTitle = document.querySelector('.figTitle').innerHTML;
var cates = document.querySelectorAll('#cate option').values;
var searchResult = searchProd;
var cateResult = searchCate;

function searchProd(prodSearch){
    for(let i=0;i < figures.length;i++){

    }
}
function searchCate(){
    
}